const inquirer = require('inquirer');
const fs = require('fs');

// this is how the game was initialized in previous module. I need another js file to hold the prompt questions ??
// const Game = require('./lib/Game');

// new Game().initializeGame();